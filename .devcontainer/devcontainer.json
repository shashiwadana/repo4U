{
    "name": "Python 3.7 Environment",
    "dockerFile": "Dockerfile",

    "postCreateCommand": "pip install pipenv && pipenv install --python 3.7",

    "remoteUser": "vscode",

    "customizations": {
        "vscode": {
            "settings": {
                // Add any VS Code settings here if needed
                "terminal.integrated.shell.linux": "/bin/bash"
            }
          
        }
    }
}
